An idea of the synopsis,
  awbot [config_options] <cmd> <parameters>

log
  log a channel
  two new config options
    logchannel - the channel to log, if not set use channel config option
    logdir - the directory to store log files in

msg priv|notice line1 [line2 [line3]]
  send a message to a channel or a user
